# 🌿 EcoTrack - Modern Carbon Calculator

![Status](https://img.shields.io/badge/Status-Completed-success)
![Tech](https://img.shields.io/badge/Tech-HTML5%20%7C%20CSS3%20%7C%20ES6+-blue)

**Select Language / Selecione o Idioma / Seleccione el Idioma:**
[🇺🇸 English](#-english) | [🇧🇷 Português](#-português) | [🇪🇸 Español](#-español)

![video](https://github.com/eumatoliveira/EcoTrip-Calculator-Environmental-Impact-Simulator-for-Travel/blob/main/V%C3%ADdeo%20sem%20t%C3%ADtulo%20%E2%80%90%20Feito%20com%20o%20Clipchamp%20(1).gif)

---

## 🇺🇸 English

A high-performance web application for carbon footprint calculation, showcasing modern web technologies (Vanilla JS, Next-Gen CSS) without framework dependencies.

### ✨ Features
*   **Comprehensive Calculation**: Evaluates emissions from Transport 🚗, Energy ⚡, Waste 🗑️, and Food 🍽️.
*   **Visual Feedback**: Emission level indicators (Low, Medium, High) with semantic colors.
*   **Premium Interface**: Glassmorphism design with smooth animations and full responsiveness.
*   **Enhanced UX**: Use of emojis for quick identification and mobile-optimized inputs (`inputmode`).

### 🛠️ Technologies & Innovations
This project utilizes cutting-edge native web development features:

**CSS (Next-Gen)**
*   **`@layer`**: CSS architecture in layers (`reset`, `theme`, `layout`, `components`) for precise cascade control.
*   **`oklch()` Colors**: Perceptually uniform color space for more vibrant and accessible color gamuts.
*   **CSS Nesting**: Native nesting syntax for cleaner and more organized code.
*   **Container Queries**: Components adaptable to their parent container, not just the viewport.

**JavaScript (Modern ES6+)**
*   **Private Class Fields (`#`)**: Real encapsulation of properties and methods in classes.
*   **ES Modules**: Native modular architecture (`import`/`export`).
*   **Optional Chaining (`?.`)**: Safe object and DOM manipulation.
*   **Intl API**: Native locale-sensitive number formatting (`Intl.NumberFormat`).

### 🚀 How to Run
Since the project uses **ES Modules**, a local server is required to avoid CORS blocks.

**Option 1: VS Code (Recommended)**
1. Install the **Live Server** extension.
2. Open `index.html`, right-click, and choose **"Open with Live Server"**.

**Option 2: Python**
```bash
python -m http.server
# Access: http://localhost:8000
```

**Option 3: Node.js**
```bash
npx http-server .
```

---

## 🇧🇷 Português

Uma aplicação web de alta performance para cálculo de pegada de carbono, demonstrando o uso de tecnologias web modernas (Vanilla JS, CSS Next-Gen) sem dependência de frameworks.

### ✨ Funcionalidades
*   **Cálculo Abrangente**: Avalia emissões de Transporte 🚗, Energia ⚡, Resíduos 🗑️ e Alimentação 🍽️.
*   **Feedback Visual**: Indicadores de nível de emissão (Baixo, Médio, Alto) com cores semânticas.
*   **Interface Premium**: Design Glassmorphism (efeito de vidro) com animações suaves e responsividade total.
*   **UX Aprimorada**: Uso de emojis para identificação rápida e inputs otimizados para mobile (`inputmode`).

### 🛠️ Tecnologias & Inovações
Este projeto utiliza recursos de ponta do desenvolvimento web nativo:

**CSS (Next-Gen)**
*   **`@layer`**: Arquitetura CSS em camadas (`reset`, `theme`, `layout`, `components`) para controle preciso de cascata.
*   **Cores `oklch()`**: Espaço de cor perceptivelmente uniforme para gamas de cores mais vivas e acessíveis.
*   **CSS Nesting**: Sintaxe de aninhamento nativa para código mais limpo e organizado.
*   **Container Queries**: Componentes adaptáveis ao seu container pai, não apenas à viewport.

**JavaScript (ES6+ Moderno)**
*   **Private Class Fields (`#`)**: Encapsulamento real de propriedades e métodos em classes.
*   **ES Modules**: Arquitetura modular nativa (`import`/`export`).
*   **Optional Chaining (`?.`)**: Manipulação segura de objetos e DOM.
*   **Intl API**: Formatação numérica nativa e sensível à localidade (`Intl.NumberFormat`).

### 🚀 Como Rodar
Como o projeto utiliza **Módulos ES6**, é necessário um servidor local para evitar bloqueios de CORS.

**Opção 1: VS Code (Recomendado)**
1. Instale a extensão **Live Server**.
2. Abra o `index.html`, clique com o botão direito e escolha **"Open with Live Server"**.

**Opção 2: Python**
```bash
python -m http.server
# Acesse: http://localhost:8000
```

**Opção 3: Node.js**
```bash
npx http-server .
```

---

## 🇪🇸 Español

Una aplicación web de alto rendimiento para el cálculo de la huella de carbono, demostrando el uso de tecnologías web modernas (Vanilla JS, CSS Next-Gen) sin dependencia de frameworks.

### ✨ Funcionalidades
*   **Cálculo Integral**: Evalúa emisiones de Transporte 🚗, Energía ⚡, Residuos 🗑️ y Alimentación 🍽️.
*   **Retroalimentación Visual**: Indicadores de nivel de emisión (Bajo, Medio, Alto) con colores semánticos.
*   **Interfaz Premium**: Diseño Glassmorphism (efecto de vidrio) con animaciones suaves y respuesta total.
*   **UX Mejorada**: Uso de emojis para identificación rápida e inputs optimizados para móviles (`inputmode`).

### 🛠️ Tecnologías e Innovaciones
Este proyecto utiliza recursos de vanguardia del desarrollo web nativo:

**CSS (Next-Gen)**
*   **`@layer`**: Arquitectura CSS en capas (`reset`, `theme`, `layout`, `components`) para control preciso de cascada.
*   **Colores `oklch()`**: Espacio de color perceptualmente uniforme para gamas de colores más vivas y accesibles.
*   **CSS Nesting**: Sintaxis de anidamiento nativa para código más limpio y organizado.
*   **Container Queries**: Componentes adaptables a su contenedor padre, no solo al viewport.

**JavaScript (ES6+ Moderno)**
*   **Private Class Fields (`#`)**: Encapsulamiento real de propiedades y métodos en clases.
*   **ES Modules**: Arquitectura modular nativa (`import`/`export`).
*   **Optional Chaining (`?.`)**: Manipulación segura de objetos y DOM.
*   **Intl API**: Formato numérico nativo y sensible a la configuración regional (`Intl.NumberFormat`).

### 🚀 Cómo Ejecutar
Dado que el proyecto utiliza **Módulos ES6**, se requiere un servidor local para evitar bloqueos de CORS.

**Opción 1: VS Code (Recomendado)**
1. Instale la extensión **Live Server**.
2. Abra `index.html`, haga clic derecho y elija **"Open with Live Server"**.

**Opción 2: Python**
```bash
python -m http.server
# Acceso: http://localhost:8000
```

**Opción 3: Node.js**
```bash
npx http-server .
```
